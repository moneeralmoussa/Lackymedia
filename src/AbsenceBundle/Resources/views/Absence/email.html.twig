<h3>Änderungen im Urlaubskalender</h3>

<p>Guten Morgen,</p>
<p>Folgende Kalendereinträge wurden seit gestern erstellt und sind noch nicht bearbeitet:</p>
<ul>
{% for item in newAbsences %}
    <li><a href="https://management.giesker-laakmann.de{{ path('absence_edit', {'id':item.id}) }}">{{ item.fromDate|date('d.m.Y') }} - {{ item.toDate|date('d.m.Y') }}</a>: <b>{{ item.reason }}</b> - {{ item.employee }}</li>
{% endfor %}
</ul>
<p>Für folgende Krankmeldungen wurde noch kein Krankenschein eingereicht:</p>
<ul>
{% for item in illWithoutCertification %}
    <li><a href="https://management.giesker-laakmann.de{{ path('absence_edit', {'id':item.id}) }}">{{ item.fromDate|date('d.m.Y') }} - {{ item.toDate|date('d.m.Y') }}</a>: {{ item.employee }}</li>
{% endfor %}
</ul>
