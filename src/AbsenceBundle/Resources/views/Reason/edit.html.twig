{% extends 'base_new.html.twig' %}
{% set title = 'Abwesenheitstyp bearbeiten' %}

{% block content %}
	<div class="row">
		<div class="grid-item col-xs-12">
			<div class="row">
				{% embed "partials/box.html.twig" with {'class': 'col-xs-12', 'title': 'Statistik' } %}
						{% block content %}
						<div class="row">
							{{ form_start(edit_form) }}
								<div class="col-xs-12">
									{{ form_row(edit_form.name) }}
								</div>
								<div class="col-xs-12">
									{{ form_row(edit_form.abbr) }}
								</div>
								<div class="col-xs-12">
									{{ form_row(edit_form.color) }}
								</div>
								<div class="col-xs-12">
									{{ form_row(edit_form.use_as_holidays) }}
								</div>
								<div class="col-xs-12">
									<input type="submit" value="Speichern" class="btn btn-info pull-right"/>
								</div>
							{{ form_end(edit_form) }}
						</div>
						{% endblock %}
						{% block footer %}
							{{ form_start(delete_form) }}
								<input type="submit" value="Löschen" class="btn btn-danger pull-right">
							{{ form_end(delete_form) }}
						{% endblock %}
				{% endembed %}
			</div>
		</div>
	</div>
{% endblock %}
