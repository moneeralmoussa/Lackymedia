"PKW-Nutzung für {{ base_date|date('M Y') }}"
"Name";"Fahrzeugtyp";"Abfahrt";"Startort";"Start KM";"Ankunft";"Zielort";"End KM";"Diff KM";"Fahrername";"Pers-Nr";"Fahrttyp";"Sauber";"Kommentar"
{% for item in vehicle_logs %}
"{{ item.vehicle.name }}";"{{ item.vehicle.vehicletype.name }} ({{ item.vehicle.comment }})";"{{ item.vehicleLogBeginTime|date('Y-m-d H:i:s') }}";"{% if item.vehicleLogBeginPosition is empty %}{% elseif not item.vehicleLogBeginPosition.name is empty %}{{ item.vehicleLogBeginPosition.name }}{% else %}{{ item.vehicleLogBeginPosition.lat }},{{ item.vehicleLogBeginPosition.lon }}{% endif %}";{{ item.vehicleLogBeginPosition.mileage }};"{{ item.vehicleLogEndTime|date('Y-m-d H:i:s') }}";"{% if item.vehicleLogEndPosition is empty %}";{% elseif not item.vehicleLogEndPosition.name is empty %}{{ item.vehicleLogEndPosition.name }}";{{ item.vehicleLogEndPosition.mileage }}{% else %}{{ item.vehicleLogEndPosition.lat }},{{ item.vehicleLogEndPosition.lon }}";{{ item.vehicleLogEndPosition.mileage }}{% endif %};{% if (not item.vehicleLogBeginPosition is empty) and (not item.vehicleLogEndPosition is empty) %}{{ (item.vehicleLogEndPosition.mileage-item.vehicleLogBeginPosition.mileage) }}{% endif %};{% if not item.employee is empty %}"{{ item.employee }}";"{{ item.employee.trimbleId }}"{% else %};{% endif %};"{% if not item.reason is empty %}{{ item.reason.name }}{% endif %}";"{% if item.vehicleClean %}{% trans %}Ja{% endtrans %}{% else %}{% trans %}Nein{% endtrans %}{% endif %}";"{{ item.comment }}"
{% endfor %}
