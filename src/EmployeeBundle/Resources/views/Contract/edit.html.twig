{% extends 'base_new.html.twig' %}
{% set title = 'Vertrag bearbeiten'|trans ~ ': ' ~  contract.name %}

{% block content %}
  <div class="row">
    <div class="grid-item col-xs-12">
      <div class="row">
        {% embed "partials/box.html.twig" with {'class': 'col-xs-12', 'title': 'Vertrag bearbeiten' } %}
            {% block content %}
              {{ form_start(edit_form) }}
                  {{ form_widget(edit_form) }}
                  <input type="submit" value="{% trans %}Speichern{% endtrans %}" class="btn btn-info pull-right"/>
              {{ form_end(edit_form) }}
            {% endblock %}
        {% endembed %}
      </div>
    </div>
  </div>
{% endblock %}
